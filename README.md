# customer-intake-n8n

> ⚠️ **This README is a work in progress. Content is incomplete and subject to change.**

- Install Docker and Compose
- Rename .env
- change .env variables
- docker compose build
- docker-compose up -d
- docker-compose stop
- "docker compose --profile full up -d" for ollama
- docker exec -it ollama ollama pull llama3.2:3b
- localhost:5678
- Create Owner Account
- Manual trigger create data table
- Authentication normally but for demo case not necessary
- Only German. In production should cover all languages
- docker compose up --build -d